# 로드맵 — v2.9, v3.0

이 문서는 앞으로 할 일과 그 순서, 그리고 각 항목이 끝났다고 말할 수 있는 조건을 적는다. 현재 동작의 설명은 [ARCHITECTURE.md](ARCHITECTURE.md)에, 왜 그렇게 됐는지는 [Docs/History.md](Docs/History.md)에 있다. 여기 적힌 것은 아직 코드에 없는 것뿐이며, v3.0이 나가면 이 파일은 지운다. 지켜야 할 불변식은 그때 ARCHITECTURE.md로 옮긴다.

## 원칙

기능을 늘리지 않는다. 실제로 발견된 문제를 고치고, 재현 테스트로 남기고, 이후 모든 릴리스에서 다시 검증한다. 입력 구조는 v2.9 동안 바꾸지 않는다. 앱별 예외는 지금의 세 정책(`TextDeliveryPolicy`, `ToggleExclusionPolicy`, `ClientCompatibilityPolicy`) 안에서만 관리하고 새 축을 만들지 않는다.

---

# v2.9 — 재현과 설치

## A. 이벤트 재생

### A0. 관찰 지점

지금 하니스가 볼 수 있는 것은 `FakeTextClient.calls`와 `reportedModes`뿐이다. 조합이 언제 왜 끝났는지, 어느 세션이 그것을 했는지는 보이지 않는다. `systemModeReporter`와 같은 방식으로 관찰 훅을 하나 더 연다.

- `InputSession.compositionObserver`: `(reason, clientIdentifier, committedLength)`를 받는다. 확정 문자열 자체는 넘기지 않는다.
- `PriTypeInputController.sessionObserver`: 세션 생성, `claimActiveController`의 주인 교체, 어댑터 교체를 알린다.
- 둘 다 릴리스 빌드에서 `nil`이고 호출부는 옵셔널 호출 하나다.

완료 조건: 기존 테스트가 그대로 통과하고, 벤치마크의 타이핑 경로 p50이 측정 오차 안에서 같다.

### A1. 시나리오 형식

재현 대본을 Swift 코드가 아니라 데이터로 둔다. 코드로 쓴 재현은 읽는 사람이 프레임워크를 먼저 읽어야 하고, 사례가 30개를 넘으면 서로 다른 관용구로 갈라진다.

`Tests/Scenarios/*.scn`, 한 줄 한 사건의 텍스트 형식:

```
# kakaotalk-duplicate-keydown
# 2026-09-18, KakaoTalk이 같은 keyDown을 두 번 보낸다
focus com.kakao.KakaoTalkMac
key g
key g same-event
expect marked ㅎ
expect committed ""
```

동사는 지금 하니스가 이미 하는 것들이다. `focus`, `activate-ahead`, `blur`, `deactivate`, `key`, `press`, `toggle`, `toggle-from-monitor`, `hanja`, `system-selects`, `click`, `place-caret`, `select`, `freeze-reports`, `set` (클라이언트 속성). 단언은 `expect marked`, `expect committed`, `expect calls`, `expect mode`, `expect reported-modes`.

파서와 실행기는 `PriTypeIMKHarness`에 둔다(`ScenarioRunner`). 테스트 타깃은 디렉터리를 훑어 각 파일을 하나의 `@Test`로 돌린다. 실패하면 기대와 실제 호출 기록을 나란히 출력한다.

왜 JSON이 아닌가: 이 파일들은 사람이 쓰고 사람이 읽는다. 중첩이 없고 한 줄이 한 사건이므로 diff가 곧 "무엇이 달라졌는가"다.

완료 조건: 기존 `IMKIntegrationTests` 중 순수하게 사건 나열인 것들이 `.scn`으로 옮겨지고, 코드로 남는 것은 클라이언트를 특이하게 조작하는 테스트뿐이다.

### A2. 구조 트레이스

실제 머신에서 벌어진 일을 시나리오로 옮기려면 무엇이 벌어졌는지 봐야 한다. 그런데 키 코드는 글자의 다른 이름이다. `DuplicateKeyProbe`가 이미 정한 규칙을 그대로 따른다 — 비교의 결과는 남기고 비교된 것은 남기지 않는다.

`SequenceProbe`(디버그 빌드, `defaults write com.pritype.inputmethod.v2 com.pritype.sequenceProbe -bool YES`)가 남기는 것:

| 남긴다 | 남기지 않는다 |
|---|---|
| 키의 갈래: 글자키 / 공백 / 백스페이스 / 방향키 / Return / Esc / Tab / 수정키조합 / 기타 | 키 코드, 문자, 수정키 조합의 내용 |
| 직전 키와의 시각 차 | 절대 시각 |
| 클라이언트 식별자(그 실행 동안만 유효한 일련번호) | 번들 ID는 남기되 문서·선택 영역의 내용은 남기지 않는다 |
| IMK 콜백: activate, deactivate, commitComposition, setValue | |
| 조합 종료 이유, 전달 모드, 중복 키 판정, 주인 교체 | 확정·조합 문자열 |
| 조합 중인 음절 수, 로컬 버퍼 길이 | 그 내용 |

이것으로 잡히는 것: 중복 keyDown, 늦게 도착한 deactivate, 전환과 키의 순서 뒤집힘, 앱 경계를 넘은 버퍼, 조합이 두 번 확정되는 경로. 이것으로 잡히지 않는 것: 어떤 자모가 잘못 조합됐는가. 후자는 사람이 재현해서 `.scn`에 키를 적는다. 자동 변환은 만들지 않는다 — 만들려면 키 코드를 남겨야 하고, 그러면 이 표의 오른쪽이 무너진다.

완료 조건: 트레이스 한 줄에 사용자 입력 내용이 들어갈 수 있는 경로가 없음을 리뷰로 확인하고, 프로브를 끈 빌드에서 키 하나당 비용이 `Bool` 한 번임을 확인한다.

### A3. 실제 사례 이식

아래는 이미 고쳤지만 재현 대본이 없는 것들이다. 각각 `.scn` 하나로 남긴다.

| 사례 | 고친 날 | 대본이 고정할 것 |
|---|---|---|
| KakaoTalk 중복 keyDown | 2026-09 | 같은 이벤트 두 번이 한 번으로 처리된다 |
| Google Docs 커서 질의 | 2026-09-19 | 인덱스 0을 먼저 묻고, 좌표가 없으면 후보창이 직전 좌표를 같은 클라이언트에서만 재사용한다 |
| Figma 조합 취소 | 2026-09-19 | 조합이 비는 순간 남은 자모를 확정하고 백스페이스를 앱에 넘긴다 |
| Terminal 후보창 키 | 2026-09-19 | 조합이 없어도 후보창이 Esc·Return·숫자를 처리한다 |
| 늦게 온 deactivate | 2026-09-19 | 새 입력창의 조합이 이전 컨트롤러의 비활성화에 영향받지 않는다 |
| 앱을 옮긴 뒤 한자 단어 | 2026-09-19 | 번들 ID가 바뀌면 로컬 버퍼가 빈다 |
| 클릭 뒤 한자 후보 | 2026-09-19 | 후보창 밖 클릭이 창을 닫고 버퍼를 비운다 |
| 붙여넣은 NFD 백스페이스 | 2026-09-20 | 커서 앞 NFD 음절을 NFC로 바꾼 뒤 앱이 지운다 |
| 모아치기와 표준 두벌식 | 2026-09-20 | ㅏ+ㄴ이 "ㅏ나", "아"+ㅣ가 "아ㅣ" |

## B. 입력 상태의 불변조건

### B1. 불변식

여덟 개를 이름으로 못 박는다. `HarnessInvariants`가 사건마다 검사한다.

- I1 · 키 하나, 변경 하나. 하나의 keyDown이 입력창 문서에 만드는 순 변경은 0회 또는 1회다.
- I2 · 조합은 한 클라이언트의 것. 확정은 그 조합을 시작한 클라이언트에만 간다.
- I3 · 확정 뒤에 남는 것은 없다. `finalize` 뒤 `markedRange`가 비고 엔진 버퍼도 빈다.
- I4 · 포커스를 떠나면 조합이 없다. `deactivate`와 `blur` 뒤 어느 클라이언트에도 조합 영역이 없다.
- I5 · 모드는 세어서 맞는다. 시스템 선택이 없는 구간에서 전환 횟수가 짝수면 모드는 처음과 같다.
- I6 · 키 순서. 전환보다 이벤트 시각이 이른 키는 이전 모드로 처리된다.
- I7 · 버퍼는 앱 경계를 넘지 않는다. 번들 ID가 바뀐 뒤 첫 한자 검색은 이전 앱의 음절을 보지 않는다.
- I8 · 텍스트 동치. 최종 확정 문자열은 두벌식 기준 모델(`Dubeolsik`)의 출력과 같다.

### B2. 기존 테스트에 붙이기

`HarnessInvariants`를 `IMKHarness` 안에서 기본으로 켠다. 이미 있는 통합 테스트 43개와 앞으로 쓸 모든 `.scn`이 자기 단언과 별개로 여덟 개를 함께 검사하게 된다. 새 테스트를 쓰지 않고 얻는 커버리지이고, 이 프로젝트가 2026-09-20에 배운 것 — 커버리지가 위험한 곳이 아니라 대역으로 만들기 쉬운 곳을 따라 자란다 — 에 대한 직접적인 대응이다.

### B3. 시퀀스 생성기

`InputSequenceFuzzTests`: 시드 하나에서 200-500개의 사건을 뽑아 하니스에 흘려 넣고 I1-I7을 검사한다(I8은 전환과 포커스 이동이 섞이면 기준 모델이 없으므로 결정론적 시나리오에서만 본다). 사건 분포는 실제 사용을 닮게 잡는다 — 글자키 70%, 공백·백스페이스 15%, 전환 5%, 포커스 이동 5%, 나머지 5%.

CI는 고정 시드 64개를 돌린다(수 초). 실패하면 시드와 최소화한 사건 열을 출력하고, 그 열을 그대로 `.scn`으로 저장할 수 있게 한다. 야간 잡에서 무작위 시드 10,000개를 돌린다.

완료 조건: 고정 시드 64개가 통과하고, 무작위 10,000개를 한 번 돌려 나온 위반이 모두 고쳐지거나 불변식 정의의 오류로 판명된다.

## C. 설치 실패

### C1. preinstall을 지우기에서 치우기로

지금 `preinstall`은 페이로드가 설치될지 결정되기도 전에 `/Library/Input Methods`와 모든 사용자 홈에서 앱을 `rm -rf`한다. 그래서 설치가 어떤 이유로든 페이로드를 놓으면 Mac에 입력기가 없는 상태로 끝난다. 버전 검사를 끈 것(`BundleIsVersionChecked` = NO)은 알려진 한 가지 원인을 막았을 뿐이고, 손상된 패키지와 중단된 설치에는 여전히 같은 결말이 있다.

고칠 방향:

1. `preinstall`은 기존 설치를 지우지 않고 `/Library/Application Support/PriType/backup/`으로 옮긴다. 사용자 홈의 잔존 설치도 같은 곳으로 옮긴다.
2. `postinstall`은 새 앱이 실제로 놓였는지, `CFBundleShortVersionString`이 패키지 버전과 같은지, 실행 파일이 실행 가능한지 확인한다.
3. 확인에 실패하면 백업을 제자리로 되돌리고 0이 아닌 상태로 끝낸다. 성공하면 백업을 지운다.
4. 백업이 있는 채로 다음 설치가 시작되면(앞선 설치가 중간에 죽은 경우) 먼저 복구부터 한다.

불변식은 하나다. 설치 시도가 어떻게 끝나든, `/Library/Input Methods`에는 실행 가능한 PriTypeV2.app이 있다.

### C2. CI의 설치 잡

GitHub의 macOS 러너는 sudo를 쓸 수 있으므로 실제로 `installer`를 돌릴 수 있다. `.github/workflows/install.yml`, 태그와 `main` 푸시에서 실행한다. Info.plist의 버전만 바꿔 두 개의 PKG를 만들고 아래를 순서대로 돌린다.

| 경우 | 기대 |
|---|---|
| 새 설치 | 앱이 있고 버전이 맞다 |
| 업그레이드 2.8.1 → 2.9.0 | 앱이 있고 버전이 2.9.0이다 |
| 다운그레이드 2.9.0 → 2.8.1 | 앱이 있고 버전이 2.8.1이다 |
| 같은 버전 재설치 | 앱이 있고 버전이 같다 |
| beta 2.9.0 → stable 2.9.0 | 앱이 있고 채널이 stable이다 |
| 손상된 PKG (절반에서 자름) | `installer`가 0이 아닌 값으로 끝나고, 이전 앱이 그대로 있다 |
| 설치 중 종료 (`installer`에 SIGKILL) | 앱이 있고 실행 가능하다 |

매 경우 뒤에 공통으로 확인한다. 실행 파일이 있고 실행 가능한가, `codesign --verify --strict`가 통과하는가, `pkgutil --pkg-info`의 버전이 기대와 같은가.

러너에 GUI 세션이 없으므로 `postinstall`의 `open`과 `launchctl asuser`는 아무 일도 하지 않는다. 이 잡은 파일 시스템의 결과만 본다. 재실행과 입력 소스 재등록은 D2의 실기기 검증이 맡는다.

완료 조건: 손상과 중단 두 경우가 C1 전에는 실패하고 C1 뒤에 통과한다. 통과하는 수정은 그 실패를 먼저 보여 준 뒤에 넣는다.

### C3. 업데이트 경로의 경계

`UpdateSignature`와 `UpdateChecker`는 이미 테스트가 있다. 빠진 것만 더한다.

- 매니페스트의 버전이 릴리스 태그와 다를 때 거절한다(이미 `check`가 한다 — 테스트가 없다).
- `LSMinimumSystemVersion`이 지금 macOS보다 높은 매니페스트를 거절한다.
- 다운로드가 `maximumPackageSize`를 넘으면 중단하고 스테이징을 남기지 않는다.
- 설치를 시작한 뒤 프로세스가 죽었다가 돌아왔을 때 `reportPendingInstall`이 로그를 읽어 정확히 한 번 보고한다.
- 공개키 교체 경로. 지금 Ed25519 공개키는 앱 안에 상수 하나다. 키를 잃거나 바꿔야 할 때의 절차를 `Docs/ReleaseRunnerSecurity.md`에 적는다. 서명 없는 릴리스는 릴리스 페이지를 여는 예전 동작으로 떨어지므로 사용자가 갇히지는 않는다 — 그 사실을 문서로 고정한다.

## D. 서명과 공증

### D1. Developer ID

지금 앱은 자체 서명 인증서 `PriType Release`로, PKG는 서명 없이 나간다. 사용자는 처음 설치할 때 시스템 설정에서 한 번 허용해야 한다. 공개 배포를 넓히려면 Apple Developer Program(연 99달러)에 들어가 Developer ID Application과 Developer ID Installer를 받는다.

`build_release.sh`는 이미 `APP_SIGN_IDENTITY`, `PKG_SIGN_IDENTITY`, `KEYCHAIN_PROFILE` 세 변수로 서명과 공증, `stapler`, `spctl` 검증까지 한다. 코드는 바꿀 것이 없고 필요한 것은 계정과 CI 비밀값이다.

비용이 하나 있다. 서명 주체가 바뀌므로 손쉬운 사용과 입력 모니터링 권한을 한 번 다시 허용해야 한다. 그래서 이 전환은 한 번만, v2.9.0의 경계에서 하고 릴리스 노트에 명시한다.

### D2. 릴리스 게이트

`release.yml`에 더한다.

- stable 태그인데 `PKG_SIGN_IDENTITY`나 `KEYCHAIN_PROFILE`이 없으면 릴리스를 만들지 않고 실패한다(비밀값이 갖춰진 뒤에 켠다).
- `xcrun stapler validate`와 `spctl -a -vv -t install`을 통과해야 릴리스를 올린다.
- 릴리스 노트에서 "공증을 받지 않았습니다" 문단을 지운다.
- C2의 설치 잡을 릴리스 전에 돌린다.

### D3. Ed25519 매니페스트는 유지

공증과 역할이 다르다. 공증은 macOS가 설치 시점에 확인하는 것이고, 매니페스트 서명은 PriType이 `installer`를 부르기 전에 스스로 확인하는 것이다. 앱의 코드 서명은 PKG 안에서 root로 도는 스크립트를 보증하지 못하므로 이 확인은 공증이 있어도 남는다. Sparkle로 교체하지 않는다 — Sparkle은 앱 번들을 제자리에서 갈아 끼우는 것을 전제하는데, PriType은 `/Library/Input Methods`에 root 권한으로 설치되므로 결국 같은 권한 경로를 써야 하고, 신뢰 부분은 이미 있다.

## E. 실기기 검증

### E1. 설치 뒤 검사

`pritype-device-check`에 설치 결과를 묻는 검사를 더한다.

- `installed-bundle`: `/Library/Input Methods/PriTypeV2.app`이 있고, 버전이 기대와 같고, 서명이 유효한가.
- `installed-signature-identity`: 서명 주체가 직전 설치와 같은가(다르면 권한을 다시 허용해야 한다는 뜻이므로 정보로 보고한다).
- `relaunched`: 설치 뒤 PriTypeV2가 사용자 세션에서 돌고 있는가.
- `input-source-after-update`: 업데이트 뒤에도 입력 소스가 켜진 채인가.

### E2. 호환성 원장

지금 앱별 예외는 세 파일에 흩어진 문자열 집합이다. 어느 것이 왜 거기 있는지는 CHANGELOG를 뒤져야 안다. 다른 macOS 입력기에서 배울 것은 기능의 수가 아니라 이것을 쌓아 두는 방식이다.

`CompatibilityLedger`: 번들 ID마다 증상, 발견일, 어느 정책이 대응하는지, 어느 테스트가 고정하는지를 담은 값의 배열. 테스트가 두 방향을 모두 검사한다. 정책에 있는 모든 번들 ID가 원장에 있고, 원장의 모든 항목이 가리키는 테스트가 실제로 존재한다. `Docs/Compatibility.md`는 이 배열에서 생성하고, 생성물이 커밋된 것과 다르면 CI가 실패한다.

완료 조건: 지금 코드에 있는 번들 ID 전부가 원장에 항목을 가진다. 증상을 적을 수 없는 항목은 그 자리에서 지운다 — 이유를 기억하지 못하는 예외는 예외가 아니라 잔해다.

## v2.9 순서와 관문

1. A0, A1, B1, B2 — 관찰 지점, 시나리오 형식, 불변식. 여기까지는 동작을 바꾸지 않는다.
2. A2, A3 — 트레이스와 실제 사례 이식.
3. B3 — 생성기. 여기서 나오는 위반을 고친다. 입력 경로를 고치는 것은 이 단계뿐이다.
4. C1, C2 — 설치. C2를 먼저 써서 손상·중단이 실패하는 것을 본 뒤 C1을 넣는다.
5. C3, E1, E2.
6. D1, D2 — 계정과 비밀값이 준비되는 대로. 1-5와 병행할 수 있다.

관문:

- 2.9.0-beta.1: 1-4가 끝나고 CI가 전부 초록이다.
- 2.9.0: 5-6이 끝나고, 실기기 검증을 서로 다른 Mac 두 대(Apple Silicon 내장 키보드, 외장 키보드)에서 통과한다. 공증된 PKG로 새 설치와 업그레이드를 각각 한 번씩 해 본다.

---

# v3.0 — 배열, 진단, 첫인상

v2.9의 관문을 통과한 뒤에 시작한다.

## F. 키보드 배열

지금은 글자 키에 문장부호를 둔 배열을 입력을 보고 판정한다(`LatinLayoutObserver`). 정확하지만 한 박자 늦다 — 그 키를 한 번 쳐야 켜진다. `TISCopyCurrentKeyboardLayoutInputSource`와 `UCKeyTranslate`로 현재 배열이 각 키 코드에서 무엇을 내는지 직접 물어 표를 만들고, `kTISNotifySelectedKeyboardInputSourceChanged`에서 다시 만든다. 첫 타의 예외가 사라지고, 아무도 시험해 보지 않은 배열도 같은 규칙으로 처리된다.

`LatinLayoutObserver`는 대체 경로로 남긴다. 클라이언트가 자판을 바꿔 잡는 경우(영문 모드의 `overrideKeyboardWithKeyboardNamed`)에는 TIS가 알려 주는 것이 실제와 다를 수 있다.

완료 조건: US, AZERTY, Dvorak, Colemak, 독일어, 노르웨이어에서 한국어 모드의 숫자와 문장부호 전체를 표로 고정하는 테스트. 실기기에서 배열을 바꿔 가며 확인.

## G. 진단 화면

설정에 새 설정을 더하지 않고 진단을 더한다. `PriTypeDeviceCheck`의 비대화형 검사를 앱 안에서 그대로 돌려 보여 준다 — 무엇이 건강한지에 대한 정의가 한 곳에 있게 된다.

보여 줄 것: 손쉬운 사용·입력 모니터링 권한, 지금 쓰는 키 감지 경로(탭 / IOKit)와 탭이 꺼진 횟수, 현재 입력 소스와 PriType 모드, 앞에 있는 앱의 전달 방식, 한자 사전 상태, macOS와 PriType 버전, 서명 주체.

복사 버튼은 이 표를 평문으로 만든다. 사용자가 친 글자, 문서 내용, 앱 목록 이외의 것은 들어가지 않는다. A2의 트레이스와 같은 규칙을 따른다.

## H. 첫 설치 경험

지금은 `postinstall`이 시스템 설정의 입력 소스 화면을 연다. 사용자는 거기서 무엇을 해야 하는지 스스로 알아내야 한다. 첫 실행 창을 하나 두고 세 단계(입력 소스 추가, 손쉬운 사용, 입력 모니터링)를 적되, 각 단계가 실제로 충족되는 순간 표시가 바뀌게 한다. 안내문이 아니라 상태다.

## I. 최소한의 앱별 설정

새 축을 만들지 않는다. 지금 있는 세 축(전달 방식, 전환키 제외, 후보창)에 대해서만 앱별 예외를 사용자가 직접 넣고 뺄 수 있게 한다. E2의 원장이 기본값을, 사용자 설정이 그 위를 덮는다.

---

# 지금 하지 않는 것

아래는 미루는 것이지 버리는 것이 아니다. 입력 경로가 충분히 안정된 뒤에 다시 본다.

| 항목 | 지금 하지 않는 이유 |
|---|---|
| 한자 학습(빈도 기억) | 사용자별 상태가 생기면 한자 경로의 재현이 상태에 의존하게 된다. 재현 체계가 먼저다 |
| 사용자 사전 | 같은 이유. 더해서 저장 형식과 이관이 새 실패 면이 된다 |
| 세벌식 | 두벌식의 불변식(I8의 기준 모델)이 배열마다 하나씩 필요해진다. 배열 하나를 확실히 한 뒤에 |
| 설정 동기화 | 동기화할 만큼 설정이 많지 않다 |
| Sparkle 교체 | D3 참고 |
| 남은 `@unchecked Sendable` 12개 | 탭 경계 여섯 개는 정리됐다. 나머지는 IMK가 메인에서만 부른다는 전제로 사는 타입들이며, 구조를 바꾸지 않기로 한 동안은 그대로 둔다 |
