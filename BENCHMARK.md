# 벤치마크

`swift run -c release PriTypeBenchmark`로 측정했다.

| 항목 | 값 |
| --- | --- |
| PriType | v2.7.4 이후 main (mmap 한자 사전) |
| 빌드 | Release |
| 배포 타깃 | macOS 14.0+ (`LC_BUILD_VERSION minos 14.0`) |
| 측정 환경 | Apple M5, 24GB RAM |
| OS | macOS 27.0 (26A428) |
| Swift | Apple Swift 6.4 |
| 벤치마크 소스 | `Sources/PriTypeBenchmark/main.swift` |

## 요약

| 항목 | 결과 |
| --- | ---: |
| 한자 사전 최초 로딩 | 1.37ms (이전 1002.48ms) |
| 한자 사전 메모리 | +3.2MB (이전 +68.6MB) |
| 한자 검색 20키 (첫 조회) | 0.27ms |
| 한자 검색 버스트 10,000회 | 5.38μs/op |
| 자모 검색 버스트 10,000회 | 0.05μs/op |
| 초성 자모 변환+검색 버스트 10,000회 | 0.19μs/op |
| 동시 검색 | 40,000회, 에러 0건 |
| 커서 좌표 검증 | 10개 케이스 통과 |
| `isValidCursorRect` 버스트 100,000회 | 0.38μs/op |
| 최종 메모리 증가 | +11.8MB (이전 +74.8MB) |

벤치마크 종합 결과는 `ALL TESTS PASSED`다.

## 한자 검색

`HanjaManager.search(key:)`의 성능이다. 사전은 `Tools/hanja/hanja.txt`를 `PriTypeHanjaCompiler`로 미리 컴파일한 정렬 바이너리 `hanja.dat`(6.9MB, 키 222,709개)이며, 앱은 이 파일을 메모리 매핑해 이진 탐색한다. 파싱할 것이 없으므로 로딩은 파일 열기와 오프셋 표 검증뿐이고, 검색이 건드린 페이지만 메모리에 올라온다. 결과 캐시는 없다.

| 항목 | 수치 | 설명 |
| --- | ---: | --- |
| 사전 로딩 | 1.37ms | 매핑과 오프셋 표 검증. 이전에는 `hanja.txt`를 파싱해 트라이를 만드는 데 1002.48ms가 걸렸다 |
| 검색 20키 (첫 조회) | 0.27ms | 해당 페이지를 처음 읽는 조회 |
| 검색 20키 (재조회) | 0.11ms | 페이지가 이미 메모리에 있는 조회 |
| 검색 버스트 10,000회 | 5.38μs/op | "가"(125개 후보) 반복 검색. 결과 문자열 생성 비용이 대부분 |
| 40키 순환 × 100회 | 4.32ms | 서로 다른 키 4,000회 검색 |

검색 결과 수 참고:

| 키 | 결과 수 | 키 | 결과 수 |
| --- | ---: | --- | ---: |
| 가 | 125 | 바 | 0 |
| 나 | 45 | 사 | 299 |
| 다 | 13 | 아 | 108 |
| 라 | 73 | 자 | 191 |
| 마 | 76 | 차 | 112 |

## 자모 특수문자 검색

자음 입력 후 한자키를 누르면 특수문자 후보를 표시한다. `jamo_symbols.json`은 14개 자음, 총 390개 특수문자를 담고 있다.

libhangul은 preedit 문자를 초성 자모(Choseong Jamo, U+1100~U+1112)로 반환할 수 있고, JSON 키는 호환 자모(Compatibility Jamo, U+3131~U+314E)를 사용한다. `HanjaManager`는 초성 자모를 호환 자모로 변환한 뒤 검색한다.

| 항목 | 수치 | 설명 |
| --- | ---: | --- |
| JSON 최초 로딩 포함 검색 | 0.38ms | 최초 자모 한자키 입력 시 1회 |
| 전체 14키 검색 | 0.00ms | 14개 자음 전체 순회 |
| 버스트 10,000회 (ㅁ) | 0.05μs/op | 호환 자모 직접 검색 |
| 초성 변환+검색 14키 | 0.01ms | U+1100 계열 초성 자모 변환 포함 |
| 초성 변환+검색 10,000회 (ᄆ→ㅁ) | 0.19μs/op | 초성 → 호환 자모 변환 포함 |

자모별 특수문자 수:

| 자음 | 수 | 자음 | 수 | 자음 | 수 | 자음 | 수 |
| --- | ---: | --- | ---: | --- | ---: | --- | ---: |
| ㄱ | 32 | ㄹ | 21 | ㅅ | 15 | ㅊ | 14 |
| ㄴ | 18 | ㅁ | 58 | ㅇ | 15 | ㅋ | 14 |
| ㄷ | 42 | ㅂ | 28 | ㅈ | 20 | ㅌ | 48 |
|  |  |  |  |  |  | ㅍ | 29 |
|  |  |  |  |  |  | ㅎ | 36 |

14개 호환 자모와 14개 초성 자모의 결과 수가 모두 일치했다.

## 동시성

`HanjaManager`를 여러 스레드에서 동시에 검색해 thread safety를 검증한다.

| 항목 | 수치 | 설명 |
| --- | ---: | --- |
| 순차 5,000회 | 5.64ms, 에러 0건 | 한자+자모+초성 키 혼합 검색 |
| 동시 5,000회 × 8스레드 | 18.89ms, 에러 0건 | 총 40,000회 동시 검색 |

매핑된 사전은 읽기 전용이라 검색끼리 잠글 것이 없다. 로딩 상태와 자모 테이블만 잠금으로 보호한다. 실제 IMK 입력 경로는 주로 메인 스레드에서 호출되지만, 방어적으로 동시 접근을 검증한다.

## 커서 좌표 검증

`HangulComposer.isValidCursorRect()`는 한자 후보창 위치 결정 시 앱이 반환한 좌표가 유효한지 판별한다. Chromium 계열 앱은 `firstRect(forCharacterRange:)`에서 쓰레기값이나 영점 좌표를 반환할 수 있어 이를 걸러낸다.

| 입력 | 기대 | 결과 | 설명 |
| --- | --- | --- | --- |
| (607, 637, w=1, h=19) | valid | pass | 정상 커서 좌표 |
| (458, 978, w=0, h=18) | valid | pass | Accessibility API fallback. width 0 허용 |
| (100, 300, w=10, h=20) | valid | pass | 일반 좌표 |
| (0, 0, w=0, h=0) | invalid | pass | Chromium 좌표 조회 실패 |
| (1.6e-314, 95886, ..., h=-1) | invalid | pass | 초기화되지 않은 메모리값 |
| (-100, 500, w=10, h=20) | invalid | pass | 주 화면만 있을 때 화면 밖의 음수 x |
| (-100, 500, w=10, h=20) + 왼쪽 보조 모니터 | valid | pass | 보조 모니터의 음수 x |
| (500, 500, w=10, h=-5) | invalid | pass | 음수 height |
| (100000, 500, w=10, h=20) | invalid | pass | 비현실적 x |
| (500, 100000, w=10, h=20) | invalid | pass | 비현실적 y |

10개 케이스 전체 통과(고정 화면 배치 기준: 1920×1080 주 화면, 필요 시 왼쪽 보조 모니터). 버스트 100,000회에서 0.38μs/op.

## HangulComposer

| 항목 | 수치 | 설명 |
| --- | ---: | --- |
| 인스턴스 생성 | 0.07ms | libhangul 컨텍스트 초기화 포함 |
| 입력모드 전환 10,000회 | 0.01μs/op | `setInputMode(composer.inputMode.toggled)` 호출. 상태 바 업데이트 제외 |

## 메모리

| 단계 | 사용량 | 증가 |
| --- | ---: | ---: |
| 프로세스 시작 | 7.6MB | - |
| 한자 사전 로딩 후 | 10.9MB | +3.2MB |
| 자모 테이블 추가 후 | 12.8MB | +2.0MB |
| 벤치마크 종료 후 | 19.4MB | +11.8MB |

한자 사전은 매핑된 파일이라 검색이 건드린 페이지만 상주하며, 메모리가 부족하면 macOS가 언제든 버렸다가 다시 읽는다. 이전 트라이 방식은 사전만으로 +68.6MB였다.

## 검증 상태

벤치마크 실행 중 다음 항목이 모두 통과했다.

- 자모 매핑 일치
- 순차 검색 안전성
- 동시 검색 안전성
- 커서 좌표 유효성 판별
- PriTypeBenchmark 실행 파일의 macOS 최소 버전 `14.0` 확인
